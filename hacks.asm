; Rena's various hacks can be toggled/configured here.
; Note that changing these settings and then loading an existing save file might
; have various issues from not being able to walk at all to just immediately
; crashing the game.

; use faster walking speed all the time?
HACK_WALK_FASTER_ALWAYS EQU 1

; hold B to walk faster? (ignored if above is enabled)
; DON'T USE THIS HACK. it's buggy; if you mash B mid-step you'll run right out
; of the map and break everything. (the "walk fast all the time" hack works
; fine, just use it)
; HACK_HOLD_B_TO_RUN EQU 1
